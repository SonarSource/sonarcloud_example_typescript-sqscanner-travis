{
  "name": "io.sonarcloud.examples.typescript-sqscanner-travis-project",
  "version": "1.0.0",
  "description": "TypeScript SQ-Scanner-based project analyzed on SonarCloud using Travis",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p .",
    "test": "jest --coverage",
    "tslint": "tslint -t json -o tslint-report.json --project . --force"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^21.2.1",
    "jest-sonar-reporter": "^2.0.0",
    "ts-jest": "^21.1.3",
    "tslint": "^5.10.0",
    "typescript": "^2.5.3"
  }
}
